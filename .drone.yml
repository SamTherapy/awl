kind: pipeline
type: docker
name: default
steps:
  - name: lint
    image: denoland/deno:debian
    commands:
      - deno fmt --check
      - deno lint
  - name: test
    image: denoland/deno:debian
    commands:
      - deno test -A